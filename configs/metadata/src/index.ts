export const APP_NAME = 'DishCover'

// Android package name does not allow dash(-), while iOS bundle identifier does not allow underscore(_).
// So make sure app_slug only contains alphanumeric characters.
export const APP_SLUG = 'dishcover'
export const APP_PACKAGE_NAME = `com.xu.${APP_SLUG}.app`
