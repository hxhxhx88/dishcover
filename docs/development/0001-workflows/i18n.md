# I18n workflow

- Run `task i18n` to extract all translatable texts. The extracted files will appear in `packages/i18n/locales`.
- Complete any missing translations. Otherwise, `task build` will not pass.
